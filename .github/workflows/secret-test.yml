name: Secret Test

on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Read secret
        env:
          TEST_VALUE: ${{ secrets.GH_TUTORIAL_TEST }}
        run: |
          echo "Secret is available to this workflow."
          echo "Length: ${#TEST_VALUE}"
